// Entry point for the build script in your package.json
import "@hotwired/turbo-rails"
import "./controllers"
import "./controllers/selected_category_controller";
import * as bootstrap from "bootstrap"
window.bootstrap = bootstrap
